import skills.*
import lang.*

class skills.SkillStateInfo {
	var duration: Number
	var mc: MovieClipInfo
	
	function SkillStateInfo(duration: Number, mc: MovieClipInfo) {
		this.duration = duration
		this.mc = mc
	}
}